/* ============================================================================
   Anchor · config.js — cloud connection settings.
   Paste your Supabase project's URL and anon (publishable) key here.
   Both values are safe to commit publicly — the anon key is designed to be
   public, and row-level security keeps each user's data private.
   Until these are filled in, Anchor runs local-only (no accounts, no sync).
   Full setup walkthrough: docs/CLOUD-SETUP.md
   ========================================================================== */
window.ANCHOR_CONFIG = {
  SUPABASE_URL: 'https://yacoefwhigagrwwtxjgn.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_g3bjaVuanQTaQri4BBen8w_P7PyCZpD'
};
